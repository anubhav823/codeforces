import java.util.HashMap;
import java.util.Scanner;

public class Lapindrome {
public static void main(String[] args) {
	Scanner sc=new Scanner(System.in);
	while(sc.hasNext())
	{
		int t=sc.nextInt();
		for(int i=0;i<t;i++)
		{
			HashMap<Character, Integer> hleft=new HashMap<>();
			HashMap<Character, Integer> hright=new HashMap<>();
			int mid=0;
			String s=sc.next();
			if(s.length()%2==0)
			{
				mid=s.length()/2;
				
			}
			else 
			{
					
			}
			}
		}
	}
}
